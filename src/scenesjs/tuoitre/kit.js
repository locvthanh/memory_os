// Moved up one level: this kit is shared by every procedural scene under
// src/scenesjs/ (Pho Bao Sang was simply the first one to need it). The code
// now lives in ../kit.js; this file stays so the tuoitre modules' imports are
// untouched.
export * from '../kit.js';
